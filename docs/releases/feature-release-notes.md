# Feature Release Notes

## 2026-09

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-09-23 | fork multi-Agent distribution surface (`feat/public-distribution`, first fork version `1.2.1-anton.1`) | The fork can be installed, discovered, and promoted independently: users no longer need to install the upstream `@opensymph/open-computer-use` to get fork features; Codex / Claude Code / OpenCode / Gemini / Cursor / ZCode / any stdio MCP host has a clear installation path, and the skill can be installed directly from this repository via `npx skills add`. | Fork branding and attribution (bilingual README, marketplace/plugin manifest, skill install docs, with the upstream copyright and PR #2/#3 links fully preserved); fork npm packaging as `@antonkhakhalin/open-computer-use` (`build-packages.mjs` fork package name/URL/`mcpName`/launcher reinstall notice); added `install-cursor-mcp.sh` (standard `mcp.json` format, user-level default, `--scope project`) and `install-local-runtime.sh` (source build + symlink, never overwrites existing files); TCC identity recognition now includes the fork npm package name (`Permissions.swift`); release-notes validator changelog link points to the fork repository; added `mcp-registry/server.json` (`io.github.AntonKhakhalin/open-computer-use`, locally validated against the official 2025-12-11 server schema); version sources uniformly bumped to `1.2.1-anton.1` (codex plugin.json / Swift / both Go runtimes / smoke suite), plugin surface 1.0.4→1.0.5; added `docs/PUBLIC_LAUNCH.md` external-facing copy and `docs/releases/github/v1.2.1-anton.1.md` English release notes. |
| 2026-09-22 | macOS window2 window identity resolution (same-bounds windows + closed-window ghost) | Closes two window2 correctness gaps: multiple windows of the same app with exactly identical bounds no longer cause `get_window_state` to return window A's screenshot paired with window B's accessibility tree (identity is resolved precisely by CGWindowID); `get_window` and all window-directed actions no longer echo leftover full CGWindow entries of closed windows as live (reporting the official stale error); minimized windows resolve as usual, and windows whose identity cannot be verified are never wrongly rejected. | Added `AXWindowIdentitySPI.swift` (runtime dlopen/dlsym resolution of the private symbol `_AXUIElementGetWindow`, with capability degradation when it is missing; the unpublished ABI is converged into a single file following the `SkyLightSPI` pattern as a review boundary); `matchAXWindow` returns `AXWindowMatch` (exact identity hit → unique frame hit → title/focused tie-break; when multiple same-bounds candidates cannot be uniquely determined, an explicit `ambiguousWindow(id): ...` error is raised instead of guessing the first candidate); a 4th AX-identity liveness check step was added to the window resolution sequence (`.absent` → same error string as stale-window; `.indeterminate` never rejects; no alpha/layer/bounds heuristics used); added 7 unit tests (pinning the three identity-seam states / same-bounds identity regression / closed-window ghost regression / minimized guard / pinning the ambiguous error string format); the fixture gained `fixture-second-same-bounds` and `minimize_window`/`restore_window` commands, and the smoke suite gained W11/W12; added design doc `docs/design-docs/macos-window-identity.md`; ARCHITECTURE / usage references (both copies under skills + plugins) / QUALITY_SCORE synchronized. |
| 2026-09-22 | macOS window2 native implementation (window management + window-directed actions) | AI agents can now operate multi-window applications on macOS with per-window precision: `list_windows` enumerates real windows (CGWindowID; titles depend on Screen Recording authorization), `get_window`/`get_window_state` read a single window's accessibility tree and screenshot (screenshot id lifecycle management, stale ids rejected before execution), `activate_window` brings the specified window to the foreground and verifies back, and all seven action tools support `window` targeting plus a `screenshotId` coordinate observation gate (observation has a screenshot / bounds unchanged / point inside pixels); behavior aligns with Windows and no longer relies on app-level key-window guessing. | Added `WindowManagement.swift` (`CGWindowListCopyWindowInfo` window directory; stale window → process exit → deny-list resolution order; unminimize + `NSRunningApplication.activate` + AXRaise/AXMain/AXFocused activation verification, no AppleScript/shell, no extra feature gate); window2 API on `ComputerUseService` (`get_window_state` pretty-printed JSON, `shot-{windowID}-{n}` screenshot id cache with invalidation on successful actions, coordinate observation gate), window targeting support in `AccessibilitySnapshot`/`InputSimulation`, and dispatcher routing of the 14-tool surface; added 46 unit tests (multi-window / same-named windows / stale id / process exit / deny list / activation switch / screenshot id lifecycle / operations during termination); multi-window fixture with 4 window2 commands, and the smoke suite gained W1–W10 (gated by `OPEN_COMPUTER_USE_SMOKE_WINDOW2=1`); README (incl. zh-CN), ARCHITECTURE, SKILL, and usage reference docs updated in sync. |
| 2026-09-22 | macOS `type_text` fallback chain fix | When the AX settable query for the focused element made by `type_text` errors out (e.g. -25205), it no longer throws the raw AX error directly and short-circuits the keyboard fallback chain; instead it treats the value as "settable not confirmed" and continues the documented fallback order (AXValue write-back → keyboard fallback → explicit error); the hard-failure gate for `set_value` is unchanged. | `typeTextBySettingFocusedValueIfAvailable`/`canTypeTextUsingKeyboardFallback` now use the non-throwing gate `typeTextValueIsSettable` (query error = settable not confirmed); added unit test `testTypeTextValueGateTreatsAXQueryErrorsAsNotSettable` pinning the gate boundary; ARCHITECTURE error path documentation synced; reproduced on the dev machine before the fix (raw -25205 thrown straight up); after the fix, appending to a TextEdit document was verified to succeed (AXValue write-back path). |
| 2026-09-20 | macOS `launch_app` native implementation | AI agents can launch macOS apps directly via the `launch_app` MCP tool, with no more shell command workarounds like `open -a Safari`. Supports resolution by app name, Bundle ID, or full path (including paths with spaces); running instances are reused directly instead of launching again; background launch does not steal the user's foreground focus; password managers continue to be denied by the built-in deny list; returns `pid`/`bundleIdentifier`/`name`/`windows[]` (`CGWindowID`; titles depend on Screen Recording authorization, window ids are returned even without it). | macOS `AppDiscovery.launch` (NSWorkspace/LaunchServices, `NSWorkspace.OpenConfiguration.activates=false`, no AppleScript introduced, no new feature gate), the `ComputerUseService.launchApp` JSON contract, and dispatcher routing of `launch_app` from "not supported yet" to the native path; added 7 unit tests (dispatch / parameter validation / nonexistent app / empty query / deny list / real launch + reuse); smoke suite aligned to the 14-tool surface; README (incl. zh-CN), SKILL, ARCHITECTURE, and usage reference docs updated in sync. |

## 2026-08

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-08-26 | record polish default compositor and click alignment | The yellow ring and the clean-room frame compositor are on by default; event coordinates are aligned to the video resolution; click/typing moments are no longer eaten by the idle fast-forward; browser address-bar click→type flows have more responsive zoom; Computer Use coordinate scaling and the record/polish capabilities across all three platforms are closed out to a releasable state. | Released `1.2.0`: Linux/Windows compositor default engine + yellow ring; `alignEventLogToVideo`, action hold, Followed skipping wait; API size / mouse_down/up / proxy / anyos; real-desktop baidu.com e2e. |
| 2026-08-25 | record polish event log and post-processing | On all three platforms, `record` writes `<stem>.events.json` during recording, and uses an open-source ffmpeg+ASS pipeline (click ripples / keystroke captions / idle speedup / smart zoom / cursor ghost) for Cursor RecordScreen-style post-processing; the `start`/`stop --polish` and standalone `record polish` subcommands are available. | Linux/Windows landed `record_events.go` + `record_polish.go` first; macOS OpenComputerUseKit synced the `DesktopCommands` event sidecar, ASS/filter_complex polish, CLI help, and unit tests. The proprietary polished-renderer is not invoked. |
| 2026-08-25 | polish refinement toward the proprietary renderer | Linux/Windows PNG fine-ring ripples (removing the large ASS filled blocks), multi-segment zoom, click importance, LOADING_WAIT/THINKING_PAUSE idle classification, mellow/quick/rapid spring cursors; macOS ASS switched to a transparent-filled stroked ring; `--cursor-style`. References the polished-renderer.node symbols and recording-renderer constants; does not link the proprietary .node. | All three platforms synced; visually fixed the "big blob" click block. |
| 2026-08-25 | polish alignment with the real compositor | Checked against the polished-renderer source in cursor-agent-architecture: the proprietary compositor does not draw the yellow ring; the clean-room implementation provides Screen Studio cursor easing, click depress, and 700ms zoom ease-in/out; ripples are off by default (`--ripples` is opt-in). Proprietary source is not vendored. | Linux/Windows/macOS |
| 2026-08-25 | polish frame compositor full feature set | Linux/Windows default `--engine compositor`: idle time remapping → zoom → lens warp → camera motion blur → cursor (depress + cursor motion blur) → keystroke chips; `--engine ffmpeg` retains the old filters; macOS still uses ffmpeg+ASS and accepts `--engine` for CLI parity. Proprietary source is not vendored. | Landed on Linux/Windows; macOS CLI parity |
| 2026-08-25 | Computer Use 1–5 + three-platform e2e | Coordinate API↔display scaling, click/scroll modifier keys, type chunking / newline→Return, key --hold-ms, mouse_down/up, instrumentation after event completion; polish SVG hotspots / CursorType / keystroke shadows; render-plan.json export/consumption; `--quality anyos` + 120fps; `record proxy` (crf17/keyint=1); Linux/Windows/macOS CLI parity; real-desktop recording + compositor verification. | All three platforms |
| 2026-08-25 | Three-platform display-level desktop commands | Windows and macOS gained full-screen CLIs aligned with Linux: the same set of `screenshot` / `cursor-position` / `input` / `record` commands can observe, control, and record the entire desktop cross-platform, so scripts no longer need per-platform branching. | Released `1.1.0`: on Windows, `screenshot` (GDI virtual desktop full screen), `cursor-position` (GetCursorPos JSON), `input` (reusing the SendInput layer, gated by `OPEN_COMPUTER_USE_WINDOWS_ALLOW_FOREGROUND_INPUT=1`, Windows/Meta keys hard-denied), `record` (ffmpeg gdigrab + Ctrl+Break graceful shutdown, experimental); on macOS, the same-named commands use CGEvent / `CGDisplayCreateImage` multi-display compositing / `screencapture -v`, added the `OPEN_COMPUTER_USE_MACOS_ALLOW_FOREGROUND_INPUT=1` gate and reused the app agent's TCC permission identity; command names, arguments, and output JSON structures are aligned across all three platforms, all are CLI-only, and the 14 MCP tool surface is unchanged; also fixed dependency-review `fail-on-scopes` empty values that caused the supply-chain CI to fail constantly. |
| 2026-08-24 | Three-platform reliability and input fixes | Windows global keyboard / multi-display mouse input is correct again, the MCP server on all three platforms no longer spins in a 100% CPU loop on bad frames, Linux invalid key combinations no longer stick modifier keys, macOS Retina hit-testing is back to correct positions, and malformed accessibility elements no longer crash the server. | Released `1.0.5`: fixed Windows `INPUT` union keyboard packing and the `VARIANT` 24-byte layout, added UIA empty pattern / panic guards, normalized virtual-screen absolute coordinates; Linux `send_key` now resolves the primary key up front and releases modifiers in reverse order; MCP stdio on both ends now reads frames line by line; macOS hit-test removed the second scaling, conditional conversion of AX return values, agent socket SO_NOSIGPIPE / timeout / unlink inode validation; snapshot caches on all three platforms dropped screenshots and gained a cap, click_count/pages are clamped, and invalid mouse_button values now produce explicit errors. |
| 2026-08-08 | Linux AT-SPI text capability detection | In PyGObject environments such as Ubuntu 24.04, `get_app_state`, `type_text`, and `set_value` no longer crash because of the missing non-standard `Accessible.is_text` / `is_editable_text` properties. | The Linux bridge now detects `Text` / `EditableText` via the standard `Accessible.get_interfaces()`, and a Python regression test that does not depend on a real desktop was added. |

## 2026-07

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-07-30 | Web clickable option boundaries | On Web pages such as Chrome, multiple text options that sit inside the same summary container each keep their own operable `element_index`; the "colleague on site", "forward to other", and "email forward" options in the BOSS Zhipin share dialog can each be targeted independently. | Released `0.3.1`, making compact generic nodes carrying `AXPress`, `AXConfirm`, or `AXOpen` text-summary boundaries that render as `button`; ordinary plain-text compression and filtering of zero-size and large-area generic nodes is unchanged. |
| 2026-07-27 | Configurable and background clicks | Callers can explicitly choose the click implementation, and perform background clicks on occluded same-Space windows on macOS, while keeping the real mouse, the foreground app, window focus, and layering unchanged. | Released `0.3.0`, adding `click_method` to `click` with `auto`, `accessibility`, `app_post`, `global`, and macOS-only `sky_click`; the default `auto` behavior is unchanged, an explicitly chosen mode no longer silently switches implementations on failure, and `global` continues to require environment authorization. |
| 2026-07-20 | Anonymous Web icon controls | Pure icon buttons on Web pages such as Chrome keep a clickable `element_index` in the snapshot even without a readable name, so agents can operate icon-only controls more reliably. | Released `0.2.1`, keeping anonymous `AXGroup` / `AXUnknown` nodes with an `AXPress` / `AXConfirm` / `AXOpen` primary action and a compact, valid frame, rendered as `button`; zero-size nodes and large-area generic click containers continue to be filtered. |
| 2026-07-08 | Snapshot budget and long-text control | Long web pages, long lists, and complex tables can raise the accessibility tree budget explicitly, and long messages or documents can be read with a larger text limit or full-text mode as needed. | Released `0.2.0`, unifying the default tree budget on all three platforms to 1200/64, and adding `max_tree_nodes`, `max_tree_depth`, and `text_limit` / `--text-limit` to `get_app_state` / `snapshot`; `show_full_text` / `--show-full-text` has been superseded by `text_limit: "max"` / `--text-limit max`. |

## 2026-06

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-06-26 | npm CLI short alias | After a global npm install, `ocu` can be used directly as the short command for `open-computer-use`, and the commonly used `ocu mcp`, `ocu call ...`, and `ocu install-codex-mcp` are friendlier for everyday typing. | Released `0.1.54`, adding `bin/ocu` to the npm staging package and reusing the existing launcher; synced the README, skill guidance, CI/CD docs, and release staging checks so future releases verify the short alias. |
| 2026-06-11 | Snapshot long text and Windows encoding stability | Default text truncation for `snapshot` / `get_app_state` is now consistent across macOS, Linux, and Windows; when a complete long message needs to be read, full-text mode can be turned on explicitly. The Windows runtime also handles non-ASCII tool arguments such as Chinese text and MCP output more stably. | Released `0.1.53`, adding `show_full_text` / `--show-full-text`, with a unified default of 500 characters and a `...` suffix; fixed Windows operation JSON reading and PowerShell output encoding; also added agent smoke and stress verification scripts. |
| 2026-06-03 | CLI element index tolerance | When hand-writing `open-computer-use call` JSON, an `element_index` written as a number still clicks, scrolls, or sets values normally, and is no longer mistaken for a missing index because of the JSON number type. | Released `0.1.52`, normalizing integer `element_index` values to strings consistently in the macOS Swift dispatcher and the Windows / Linux Go runtime, while continuing to reject empty values and decimals, and syncing tests, skill examples, and history. |

## 2026-05

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-05-20 | macOS permission completion state | Once Accessibility / Screen Recording authorization is already complete, the permission onboarding floating panel exits promptly based on the real preflight state of the currently running process, and no longer lingers because of stale TCC path query results. | Released `0.1.51`, merging TCC persistent grants with positive runtime preflight signals in `PermissionDiagnostics.current()`, and adding a local end-to-end script covering the path where onboarding exits quickly when `doctor` reports granted. |
| 2026-05-13 | macOS click and input stability | Clicking the Feishu / Lark conversation list no longer accidentally triggers the "Done" action on the right, and rich text input such as Chinese and emoji is more stable; at the same time, container/link clicks on ordinary browser pages such as Chrome/GitHub keep their original behavior without regression. | Released `0.1.50`, batching Unicode delivery by grapheme cluster in macOS `type_text`, and preferring `AXValue` writes on settable Electron input fields; the click path gained a safe left-side anchor on synthetic rows, side-action filtering, and an Electron-scoped row-level `AXPress` optimization, with Chrome/GitHub pinned-repo comparison verification to avoid browser WebArea regression. |
| 2026-05-11 | macOS file selection dialog | The file list on the right side of upload/open file dialogs can now be read and clicked, and the Open button fires correctly after selecting an image, instead of being limited to operating the directory tree on the left. | Released `0.1.49`, adding `AXContents` / `AXVisibleChildren` traversal to the macOS AX snapshot, preferring native `AXList` children in coordinate clicks, and letting the screenshot window preferentially bind to the foreground open panel in overlapping-sheet scenarios; added the corresponding unit tests and history. |
| 2026-05-08 | MCP result size control | Complex windows such as Chrome / Electron that return screenshots are less likely to trigger Codex's large-result degradation; history and upstream tool consumption keep the normal `text + image` structure, instead of stuffing the entire MCP result into a text string. | Released `0.1.48`, adaptively downscaling macOS `ScreenCaptureKit` screenshots by maximum size and target byte count before encoding them as MCP image blocks, and adding tests that lock in the large-image compression and small-image fidelity paths. |
| 2026-05-08 | Silent smoke test runs | Developers running `./scripts/run-tool-smoke-tests.sh` are no longer interrupted by the internal fixture's orange test window; regression verification is quieter and no longer makes users think an extra app popped up on the desktop. | Released `0.1.47`, starting the internal fixture in headless mode by default in the smoke suite, while preserving the fixture's discoverability in the `list_apps` smoke path. |
| 2026-05-07 | Window-unavailable error wording | In no-window scenarios such as Lark / Chrome, the return text of the open-source `get_app_state` now matches the current output of the official `computer-use`, reducing diff noise in upstream prompts, tests, and user troubleshooting. | Released `0.1.46`, unifying the missing-AX-window and missing-visible-CGWindow paths to `Apple event error -10005: cgWindowNotFound`, and adding unit tests that lock in that official-style text. |
| 2026-05-07 | Visible window matching | When an app has no visible window, the window is minimized, or an AX window cannot be matched to an on-screen CGWindow, `get_app_state` now follows the official `computer-use` `cgWindowNotFound` semantics more closely, avoiding a half-baked state without a screenshot coordinate basis. | Released `0.1.45`, based on the `cgWindowNotFound`, `noWindowsAvailable`, `matchingWindowNotFound`, and `AXWindowMiniaturized` clues from reverse-engineered strings in the official `1.0.770`, requiring a real app snapshot to satisfy both a non-minimized `AXWindow` and a visible `CGWindow`. |
| 2026-05-07 | No-available-window state alignment | When apps such as Lark / Feishu temporarily have no usable AX window, `get_app_state` now returns an explicit unavailable error instead of emitting a misleading tree with only the application / menu bar, so downstream tools do not receive non-operable element indexes. | Released `0.1.44`, applying `AXWindow` role filtering to the focused-window and first-window candidates, and requiring a real AX window before rendering the accessibility tree; this behavior aligns with the failure semantics of the official `computer-use` in the same Lark state. |
| 2026-05-07 | WebArea state tree structure | The `HTML content` area of Electron apps such as Feishu / Lark keeps a shallow layout container and branching structure closer to the official `computer-use`, reducing over-flattening and making it easier to later locate messages, announcements, and input areas by context. | Released `0.1.43`, adjusting WebArea generic wrapper elision based on comparison with the official `computer-use` `1.0.770` and binary string clues: keeping shallow containers and deep branching containers while continuing to compress deep single-child chain noise. |
| 2026-05-07 | Electron state tree detail alignment | Compact rows in Feishu / Lark message lists with avatars or images now more closely match the official `computer-use`, keeping the `container -> text + image` structure, so image child nodes are easier to locate when clicking and reading the tree afterwards. | Released `0.1.42`, rendering the summary as synthetic child text when a short-text summary container has an image descendant, and keeping up to four image child nodes; added Lark comparison samples and unit tests. |
| 2026-05-07 | Electron state tree alignment | The `get_app_state` of Electron apps such as Feishu / Lark now more closely matches the official `computer-use`: the message list, chat input box, sidebar, menu bar, and WebArea URL are all kept at the same time, and internal image resource URLs no longer leak into the readable tree. | Released `0.1.41`, enabling Electron accessibility mode based on reverse-engineering clues from the official app, adjusting AX traversal deduplication, short-text merging, menu bar rendering, boolean tabs, and image/WebArea formatting, and adding Feishu comparison verification. |
| 2026-05-07 | Feishu / Electron state stability | The `get_app_state` of Electron/WebView apps such as Feishu is more likely to return operable message content and input boxes, instead of returning only a screenshot while missing key UI tree nodes; screenshot capture exceptions also no longer hang the entire state read. | Released `0.1.40`, relaxing the macOS AX tree depth budget, compressing empty wrappers, filtering `AXScrollToVisible` and empty-string noise, and adding a timeout to ScreenCaptureKit screenshots; also letting the smoke suite test the current build artifacts directly. |
| 2026-05-07 | macOS app safety boundary | Chrome, terminals, Atlas, and system components are no longer blocked by the hard-coded denylist, reducing collateral damage to ordinary app automation paths; password managers remain built-in blocked. | Released `0.1.39`, shrinking the macOS `AppSafetyPolicy` to 1Password, Bitwarden, Dashlane, LastPass, NordPass, and Proton Pass, and syncing tests, architecture, and security docs. |
| 2026-05-06 | app-agent launch after npm install | After `npm install -g open-computer-use`, `open-computer-use doctor` no longer launches the wrong copy and times out because of a leftover old staging `.app` on the machine. | Released `0.1.38`: when the current process already comes from a legitimate `Open Computer Use.app` bundle, agent launch preferentially reuses the currently executing bundle, then falls back to other copies discovered via LaunchServices / standard install directories. |
| 2026-05-06 | macOS permission identity | After macOS users reinstall a new npm package, they only need to grant permission to `Open Computer Use.app`, with no more requirement to separately grant Accessibility or Screen Recording to iTerm / Terminal. | Released `0.1.37`, running `mcp`, `doctor`, `call`, `snapshot`, and `list-apps` through an app-agent launched via LaunchServices, so that real AX / ScreenCaptureKit calls land on the `.app` permission principal, and updating the permission wording and version sources in sync. |

## 2026-04

| Date | Feature area | User value | Change summary |
| --- | --- | --- | --- |
| 2026-04-23 | Linux Codex MCP installation | Linux users can now connect via the `npm i -g open-computer-use`, `open-computer-use install-codex-mcp`, `codex` path, without manually editing the D-Bus / display environment variables of the desktop session. | Released `0.1.36`: the Linux runtime dynamically discovers the current user's `/run/user/<uid>` session bus and display / Wayland environment before starting the AT-SPI bridge, and the Codex configuration continues to use `open-computer-use mcp`. |
| 2026-04-23 | Three-platform npm install | `npm i -g open-computer-use` now invokes the corresponding macOS `.app`, Linux binary, or Windows `.exe` based on the current `os-arch`, no longer locking npm distribution to macOS. | Released `0.1.35`: all three existing npm packages now embed the macOS, Linux, and Windows runtimes, with a cross-platform Node launcher selecting the native runtime; the new platform-package scheme from `0.1.34` was superseded after failing on npm package name permissions. |
| 2026-04-22 | Gemini / opencode MCP installation | Gemini CLI and opencode users can now complete the `open-computer-use` MCP setup with the repository's built-in commands just like Claude Code / Codex, without manually looking up each configuration format. | Released `0.1.33`, adding `install-gemini-mcp` and `install-opencode-mcp`, filling in the npm launcher, README, and shared configuration helper together, and ensuring Gemini's default project-scope configuration does not pollute the repository's git state. |
| 2026-04-22 | Release version realignment | The GitHub Release tag, the plugin manifest, and the in-repo version constants are back on the same version line; subsequent local installs, staging, and diagnostics will no longer show the confused state of "the release is already the new version, but the artifacts still display the old value". | Released `0.1.32`: given that remote `v0.1.31` already exists, `plugin.json`, the Swift version constants, the smoke/test initialization requests, and the CLI helper docs were uniformly bumped to `0.1.32`, keeping a single version source for the next release. |
| 2026-04-22 | Visual cursor landing point and idle-state closure | Coordinate clicks in Retina windows are more accurate, and the visual cursor is more stable while waiting for the next action, closer to the official look and feel. | Released `0.1.31`, fixing the screenshot-pixel to window-point mapping so `click({x,y})` no longer lands off-target on high-DPI screens; also converging the idle cursor to a 30-second dwell that stays on top of the target window, and changing the waiting state to an anchored tip + tiny rotation wobble. |
| 2026-04-22 | Windows runtime preview and release notes | Windows users and subsequent developers now have an experimental Computer Use runtime starting point; every GitHub Release also keeps a user-facing change summary, instead of leaving only a changelog link. | Released `0.1.30`, adding the experimental Go/PowerShell Windows runtime, build scripts, and docs; also clarifying that the release agent must check the GitHub auto release notes after bumping the version and manually add `What's Changed` when needed. |
| 2026-04-22 | click targeting fallback | When the target app's Accessibility click path is incomplete, `click` now first tries sending a targeted mouse event, without immediately grabbing the user's real mouse; scenarios such as the Finder sidebar are also more likely to actually click through. | Released `0.1.29`, completing the `AX -> pid-targeted mouse event -> optional global pointer fallback` order for `click`: trying `AXPress` / `AXConfirm` / `AXOpen` first, then descendant click candidates and a hit-test near the hit point, while keeping the global physical pointer as an explicit opt-in last resort. |
| 2026-04-22 | Software cursor movement speed | The default movement speed of the overlay cursor for `click` / `set_value` no longer looks too fast, matching the `Cursor Motion` default setting and the official spring endpoint-lock timing. | Released `0.1.28`, removing the old distance-compressed duration formula in the runtime; the default move now directly uses the recovered `response=1.4` / `dampingFraction=0.9` / `dt=1/240` close-enough time `343 / 240 = 1.4291667s`, and regression tests and reverse-engineering docs were added. |
| 2026-04-22 | Computer Use tool alignment closure | The schemas, error semantics, and default non-intrusive input paths of the 9 Computer Use tools now come even closer to the official ones; `press_key` supports more xdotool aliases. | Released `0.1.27`, closing out the remaining tool checklist: the `perform_secondary_action` invalid-action error was changed to the official form, the fixture `Raise` no longer prepares global physical pointer input, `press_key` was filled out with `BackSpace`, `Page_Up`, `Prior` / `Next`, `F1...F12`, and common `KP_*` aliases, and the execution plan progress items were made concrete. |
| 2026-04-22 | Computer Use operation continuity | Consecutive `click` / `set_value` calls no longer repeatedly start from a fresh cursor in the bottom-left corner, and the overlay can be explicitly cleaned up at task end; the default paths of `scroll` / `drag` further avoid moving the user's real mouse. | Released `0.1.26`: after reviewing the official bundled app, the visual cursor was changed to be retained for about 5 minutes of idle and hooked into `turn-ended` cleanup; `scroll.pages` was aligned to a number schema, empty required-string values are treated as missing, and the `scroll` / `drag` default fallback was changed to a pid-targeted event. |
| 2026-04-22 | set_value settable boundary | `set_value` now returns a clear non-settable error for text areas that cannot be set directly, such as Sublime's, instead of exposing the underlying `-25200`. | Released `0.1.25`: following the settable accessibility element semantics of the official bundled app, it checks `AXUIElementIsAttributeSettable(kAXValueAttribute)` before writing, and does not fall back to the keyboard, clipboard, or unpublished text-replacement interfaces when not settable. |
| 2026-04-22 | click non-intrusive default behavior | The AX failure path of `click` no longer moves the user's real mouse by default, and multi-clicks also prefer reusing an available AX action. | Released `0.1.24`: based on the reverse-engineering results of the official package's click/EventTap, the global physical pointer fallback was changed to an explicit opt-in via `OPEN_COMPUTER_USE_ALLOW_GLOBAL_POINTER_FALLBACKS=1`, and the problem where `click_count > 1` fell straight into the global mouse path was fixed. |
| 2026-04-21 | CLI tool orchestration | Without an MCP client, the 9 Computer Use tools can be called directly via `open-computer-use call`, and a JSON array can orchestrate consecutive actions within the same process. | Released `0.1.23`, adding the native `call` subcommand, a shared MCP/CLI tool dispatcher, `--calls` / `--calls-file` sequential execution, and the related documentation and tests. |
| 2026-04-21 | Software cursor runtime orientation | The visible orientation of the software cursor when it first appears and when it moves now more closely matches the official behavior, and the up-move, turning, and landing poses no longer show the awkwardness caused by coordinate system flipping. | Released `0.1.22`, fixing the speed/orientation conversion between AppKit global coordinates and the Cursor Motion y-down screen state in the runtime overlay, and adding regression tests that lock in the first `(0,0)` start point and the render rotation relationship. |
| 2026-04-21 | Software cursor visual consistency | The software cursor shown during `click` / `set_value` now more closely matches the Cursor Motion reference rendering, with more stable visual boundaries for first appearance, movement orientation, and the official app icon. | Released `0.1.21`, switching the runtime overlay to the shared programmatic glyph renderer, fixing the initial orientation and the runtime drawing direction, and closing out the safe margins of the Open Computer Use app icon in sync. |
| 2026-04-20 | Installer host dependency | `open-computer-use install-codex-plugin` no longer has an extra dependency on `rsync`, and the plugin installation path is closer to "works as long as npm/Node is available". | Released `0.1.20`, changing the plugin installer's implementation of copying the plugin directory and `.app` bundle from `rsync` to Node `cpSync`, continuing to close out the installer runtime prerequisites of `open-computer-use`. |
| 2026-04-20 | Installer runtime dependency | The one-shot install commands of `open-computer-use` no longer fail because the system Python version is too old, and the first setup path after a global npm install is more stable. | Released `0.1.19`, removing the runtime dependency of `install-claude-mcp`, `install-codex-mcp`, and `install-codex-plugin` on `python3` / `tomllib`, and unifying configuration read/write onto a Node helper shipped with the npm package. |
| 2026-04-20 | macOS distribution signing | The release `.app` of `Open Computer Use` can now go through a unified `Developer ID Application` signing chain. | Released `0.1.17`, connecting the `Developer ID Application` certificate import and unified signing chain. |
| 2026-04-20 | Open Computer Use dev-mode identity | Local debug/dev builds no longer get mixed up as same-named objects with the official release in the system permission list, making the boundary between development grants and official distribution clearer. | Released `0.1.16`: the CI release reverted to the original ad-hoc packaging path, no longer requiring GitHub Actions to import a development certificate; local non-release builds are uniformly renamed to `Open Computer Use (Dev).app` and `com.ifuryst.opencomputeruse.dev`, and permission discovery also preferentially binds the current dev app in dev runtime. |
| 2026-04-20 | Open Computer Use permission identity | After installing from npm, brew, a DMG, or a local build, the permission identity of `Open Computer Use.app` is now easier to converge onto the same signing chain, instead of treating the npm path by default as the only stable grant target. | Released `0.1.15`: a unified codesign entry was added to the packaging chain of `Open Computer Use.app`, and the CI release supports unified signing after importing the certificate via GitHub Actions secrets; permission discovery was also changed to search by bundle identity across the currently running copy, `/Applications`, npm, and Homebrew install locations. |
| 2026-04-19 | Permission floating panel detail closure | The onboarding floating panel after `Allow` has more coherent animations, and automatically lands in the correct position once the System Settings window stabilizes, without the user having to click manually to reposition it. | Released `0.1.12`, completing the source-to-target entrance animation, back button, and continuous re-anchoring after the animation ends for the permission panel, and fixing the release workflow publish failure caused by the npm version still being stuck at `0.1.11`. |
| 2026-04-18 | Permission floating panel and documentation entry | The permission floating panel now appears directly on the first cold start of System Settings, and the repository root also regains the Chinese documentation entry. | Released `0.1.11`, fixing the timing issue where the helper floating panel only appeared after switching windows on the first `Allow` cold start of `System Settings`, and adding the root-level `README.zh-CN.md` to carry the Chinese documentation. |
| 2026-04-18 | Permission identity and onboarding | The permission identity after npm install is more stable, and already-authorized users are no longer bothered by repeated popups. | Released `0.1.10`, unifying the bundle identifier to `com.ifuryst.opencomputeruse`, making permission detection compatible with path-based TCC records and preferentially recognizing the app after a global npm install; also letting `doctor` / the default startup skip the onboarding popup when permissions are complete, and automatically closing the window after authorization is complete. |
| 2026-04-17 | Release stability | The release workflow no longer fails early in the build stage because of Xcode 26's CoreFoundation type checking. | Released `0.1.9`, fixing the compile errors of `AXUIElement` property reads in the permission onboarding window under `macos-26` / Xcode 26.2, restoring the npm release artifact build chain. |
| 2026-04-17 | Permission onboarding and install | The permission authorization floating panel no longer drops to the bottom of the screen after `Allow`, and the repository continues to provide stable one-shot install/release versions. | Released `0.1.8`, closing out the positioning fix for the `System Settings` follower panel, and updating the version numbers in the plugin, CLI, smoke/test, and release docs in sync. |
| 2026-04-08 | Template repository | Provides an Agent-first foundational template set that can be used directly to kick off a new project. | Filled in the AGENTS entry, execution plan, history, release notes, CI/CD, and supply-chain security skeleton. |
| 2026-04-17 | Open-source computer-use | Provides a Swift `computer-use` MCP server that can run locally and be verified by regression. | Added the Swift package, 9 tools, the fixture app, the smoke suite, `doctor`/`snapshot` diagnostic entry points, and the corresponding architecture docs. |
